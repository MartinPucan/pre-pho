{
  "name": "pre-pho",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "parcel serve index.html",
    "dev:css": "parcel watch src/scss/*.scss --no-source-maps -d dist/ --no-hmr --public-url ./",
    "build:css": "parcel build src/scss/*.scss --no-source-maps -d dist/ --public-url ./",
    "dev:js": "parcel build src/js/*.js --no-source-maps -d dist/ --public-url ./",
    "build:js": "parcel build src/js/*.js --no-source-maps -d dist/ --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.42.1"
  }
}
